node_modules
frontend/.next
frontend/out
backend/.venv
__pycache__
*.pyc
.env
.DS_Store

# Local Netlify folder
.netlify

# Claude AI local settings
.claude/settings.json

# Local MT5 1m bar exports — uploaded via API, must not bloat the build context
1mdata/
1MDATA/

# Demo bot + Claude decider — KOD 2 makine arası senkronize edilir, ama
# ŞİFRELER (config) + runtime/log ASLA commit edilmez (__pycache__/*.pyc zaten üstte):
yeni deneme/config.py
yeni deneme/*.log
yeni deneme/tickdata/
claude_decider/decider_config.py
claude_decider/vix_live.json
claude_decider/memory/journal.jsonl

# Local research documents — never commit
day_structure_research.md
day_structure_analysis.png

# ── Large research DATA artifacts (2026-07-06) ──────────────────────────────
# Code/scripts/reports are committed; bulk data is not (GitHub 100MB/file limit,
# repo-bloat). Regenerate via the research scripts, or store data outside git.
*.jsonl
*.parquet
sr_rejection_research/data/
signal_performance_research/*.json
signal_performance_research/*.txt
research/cortex_confluence/**/*.json
research/cortex_backfill/**/*.json
RECOVER/*.json
RECOVER/*.csv
bot_live_audit/**/*.json
bot_live_audit/**/*.csv
macro_ndx_test/**/*.json
multi_asset_edge_research/**/*.json
claude_decider.zip
claude_decider/*.backup_*.json

# Evrim Paneli runtime durumu (tohum veri commit'lenir, biriken durum edilmez)
backend/data/evolution/runs/
backend/data/evolution/lessons.json
backend/data/evolution/session_log.json
