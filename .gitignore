node_modules
frontend/.next
frontend/out
backend/.venv
__pycache__
*.pyc
.env
.DS_Store

# Local Netlify folder
.netlify

# Claude AI local settings
.claude/settings.json

# Local MT5 1m bar exports — uploaded via API, must not bloat the build context
1mdata/
1MDATA/

# Demo bot + Claude decider — KOD 2 makine arası senkronize edilir, ama
# ŞİFRELER (config) + runtime/log ASLA commit edilmez (__pycache__/*.pyc zaten üstte):
yeni deneme/config.py
yeni deneme/*.log
claude_decider/decider_config.py
claude_decider/vix_live.json
claude_decider/memory/journal.jsonl

# Local research documents — never commit
day_structure_research.md
day_structure_analysis.png
